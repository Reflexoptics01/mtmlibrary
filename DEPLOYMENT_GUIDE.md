# Maktaba — Deployment

Follow [DEPLOYMENT.md](DEPLOYMENT.md). Create your own Supabase project, run `supabase/schema.sql`, and put only your URL and anon key in `.env.local`. There is no bundled admin account.
